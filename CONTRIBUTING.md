# 修订指南（CONTRIBUTING）

感谢你参与清屿服务器规则仓库的维护。本指南说明规则文档的修订流程、结构规范、写作要点、提交流程与发布方式，供运营团队成员及协作者参考。

## 修订流程（七日阳光流程）

1. **提交草案** —— 在 `docs/` 之外先形成书面草案，逐条列明新增、删减或修改的内容，并写明变更理由
2. **公示 7 日** —— 草案在 QQ 群公告与游戏内公告栏同步发布，开放意见收集通道
3. **表决与生效** —— 公示期内无重大反对意见的，草案自动转正并即时生效；存在重大分歧的，公示期延长至 14 日，必要时召开线上听证会
4. **登记** —— 在 `CHANGELOG.md` 登记变更条目，并在公告末尾附版本号与生效时间戳

### 紧急修订

涉及服务器安全、数据安全或严重漏洞的紧急修复，可跳过公示期直接修订并立即生效，但须：

- 在 24 小时内补发公示，说明变更内容与原因
- 在 `CHANGELOG.md` 中登记
- 接受运营团队的后续复核

## 文档结构

规则文档统一存放于 `规则/` 目录：

| 文件 | 内容 | 版本位置 |
|------|------|---------|
| `规则/清屿服务器玩家守则.md` | 全体玩家的基础行为守则与专项细则 | 文件头部 |
| `规则/清屿服务器管理员条例.md` | 管理员行为规范与违规封禁规范 | 文件头部 |
| `规则/清屿服务器地铁乘车管理条例.md` | 地铁系统乘车规范 | 文件头部 |
| `规则/处罚细目表.md` | 封禁对照表索引与使用方式 | 文件头部 |
| `规则/处罚细目/*.md` | 七大类处罚细目 | 文件头部 |
| `规则/清屿服务器玩家身份与治理条例.md` | 玩家身份体系与治理程序（单文件，含逐条通俗说明） | 文件头部 |
| `服务器信息.md` | 服务器地址、版本、官方渠道 | — |
| `README.md` | 服务器主页与索引 | — |
| `CHANGELOG.md` | 版本演进记录 | — |

## 版本号与头部格式

每份规则文档头部必须包含版本号与更新日期，格式如下：

```markdown
> **版本：** v3.3.1 ｜ **更新日期：** 2026.10.01 ｜ **生效日期：** 2026.10.08 ｜ **面向对象：** 全体玩家
```

版本号遵循语义化版本：条文实质变更升次版本号（如 v3.3.0 → v3.4.0），措辞与错别字修订升修订号（v3.3.0 → v3.3.1）。日期统一使用 `YYYY.MM.DD`。

**更新文档后必须同步更新 `README.md` 规则矩阵中的版本号**，否则一致性检查会失败。

## 提交规范

- 分支命名：`docs/<简述>`、`fix/<简述>`、`feat/<简述>`
- 提交信息：`类型: 简述`，例如 `docs: 玩家守则 8.4.1 补充刷屏判定标准`
- 一个提交只做一件事；条文改动与格式改动尽量分开提交
- 提交前运行 `npm run verify`，确保 lint 与一致性检查通过

## 写作要点

- 规则条文应**明确、可执行、可量化**：避免含糊表述，时限、数额、比例尽量给出具体数字
- 新增条款时同步考虑：是否影响封禁对照表、是否需要登记 `CHANGELOG.md`
- 条文有实质变更时，同步检查身份条例与《玩家守则》的交叉引用是否仍然成立
- 涉及服务器信息（地址、版本）变更时，同步更新 `服务器信息.md`
- 表格、编号、引用保持与既有文档一致的风格，便于读者查阅与程序校验
- 引用其他条款时写清文档名与编号，例如"见《玩家守则》8.4.1"、"参见 `L-003`"

## 分支与合入

- 主分支为 `main`，保护分支不接受直接推送
- 修订需提交 Pull Request，并在 PR 描述中说明：变更内容、变更理由、公示情况、受影响的条款
- PR 需通过 `lint` 工作流；涉及封禁规范的改动需至少一名运营团队成员复核

## 其他内容维护

- **文档站：** `mkdocs.yml` 的 `nav` 需与 `规则/` 目录结构保持一致，新增文档后同步登记
- **校对检查：** `npm run check` 会检查编号引用、章节引用、内部链接、版本一致、头部规范
- **仓库地址：** 首次发布到新仓库时，请全局替换 `README.md`、`mkdocs.yml` 中的仓库名与 Pages 地址

## 本地校验

```bash
npm install
npm run verify      # markdownlint + 一致性检查
npm run docs:serve  # 本地预览文档站
```

---

© 清屿运营团队。
